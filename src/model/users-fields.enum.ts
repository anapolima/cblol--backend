enum UsersFields
{
    $EMAIL = "$email",
    $USERNAME = "$username",
    $DOCUMENT = "$document",
    $ID = "$_id",
    $FULLNAME = "$fullname",
    $BIRTH_DATE = "$birthDate",
    $CONFIRM_EMAIL_CODE = "$confirm_email_code",

    EMAIL = "email",
    USERNAME = "username",
    DOCUMENT = "document",
    ID = "id",
    _ID = "_id",
    FULLNAME = "fullname",
    BIRTH_DATE = "birthDate",
    CONFIRM_EMAIL_CODE = "confirm_email_code",
    ENCRYPTED_PASSWORD = "encryptedPassword",
}

export { UsersFields };
